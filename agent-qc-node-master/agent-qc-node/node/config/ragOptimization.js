// ------------------------------------------------------------------------------
// 文件名称: ragOptimization.js
// 主要功能: RAG 检索词重写灰度配置
// 设计要点:
//   - 通过白名单精准控制指定规则开启 LLM 检索词重写
//   - 默认全局关闭，保证线上其他规则不受影响，实现最小爆炸半径
// ------------------------------------------------------------------------------

/**
 * RAG 检索优化配置项
 */
module.exports = {
    /**
     * 指定规则编码开启查询重写 (白名单)
     * 键为规则编码，值为 true 表示开启
     */
    enableRewriteByRuleCode: {
        'F001.009': true,
        'A005.006': true,
    },

    /**
     * 未指定的规则是否默认开启重写
     * 灰度阶段设为 false，待验证无误后可改为 true 全量推开
     */
    enableRewriteDefault: false,
}
